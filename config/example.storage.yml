test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Put in the service account json key file path 
# local:
#   service: GCS
#   credentials: '/secrets/key-file.json'
#   project: ""
#   bucket: ""